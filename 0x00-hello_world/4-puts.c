#include <stdio.h>

/**
 * main -> A function, print programming is like building a multilingual puzzle
 * Return: success always 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
